const first = 2;
const second = "2"; // If we use == then it shows true but if use === then it shows false. === check the value   and also the data type
if(first === second){
    console.log("condition is true");
}
else{
    console.log("condition is false");
}